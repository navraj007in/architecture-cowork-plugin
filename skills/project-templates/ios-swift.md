---
name: project-templates-ios-swift
description: Starter file templates and boilerplate for scaffolding native iOS apps with Swift and SwiftUI — project structure, networking, auth, push notifications, deep linking
type: skill-extension
parent: project-templates
---

# iOS (Swift / SwiftUI) Project Templates

## Native iOS — Swift / SwiftUI

**Initialization:**
Native iOS projects require Xcode. There is no CLI scaffolder — write project files directly.

**Minimum deployment target:** iOS 17.0  
**Language:** Swift 5.10+  
**UI framework:** SwiftUI  
**Package manager:** Swift Package Manager (SPM)

---

## Project Structure

```
{{ComponentName}}/
├── {{ComponentName}}.xcodeproj/
│   └── project.pbxproj            — generated by Xcode / write stub
├── {{ComponentName}}/
│   ├── App/
│   │   ├── {{ComponentName}}App.swift     — @main entry point
│   │   └── ContentView.swift              — root view
│   ├── Core/
│   │   ├── Network/
│   │   │   ├── APIClient.swift            — URLSession wrapper
│   │   │   └── Endpoint.swift             — endpoint enum
│   │   ├── Auth/
│   │   │   ├── TokenStorage.swift         — Keychain wrapper
│   │   │   └── AuthManager.swift          — token lifecycle
│   │   ├── PushNotifications/
│   │   │   └── PushNotificationManager.swift
│   │   └── DeepLinking/
│   │       └── DeepLinkHandler.swift
│   ├── Features/
│   │   └── {{Responsibility}}/
│   │       ├── {{Responsibility}}View.swift
│   │       ├── {{Responsibility}}ViewModel.swift
│   │       └── {{Responsibility}}Repository.swift
│   └── Resources/
│       ├── Assets.xcassets/
│       └── Info.plist
├── {{ComponentName}}Tests/
│   └── {{ComponentName}}Tests.swift
├── .gitignore
└── README.md
```

---

## Entry Point

**{{ComponentName}}App.swift:**
```swift
import SwiftUI

@main
struct {{ComponentName}}App: App {
    @StateObject private var authManager = AuthManager()

    init() {
        PushNotificationManager.shared.requestPermission()
    }

    var body: some Scene {
        WindowGroup {
            ContentView()
                .environmentObject(authManager)
                .onOpenURL { url in
                    DeepLinkHandler.shared.handle(url: url)
                }
        }
    }
}
```

**ContentView.swift:**
```swift
import SwiftUI

struct ContentView: View {
    @EnvironmentObject var authManager: AuthManager

    var body: some View {
        Group {
            if authManager.isAuthenticated {
                // TODO: Replace with main app navigation
                Text("{{ComponentName}}")
                    .font(.largeTitle)
            } else {
                // TODO: Replace with login view
                Text("Please log in")
            }
        }
    }
}
```

---

## Networking

**Core/Network/Endpoint.swift:**
```swift
import Foundation

enum Endpoint {
    case health
    // TODO: Add endpoints matching the OpenAPI contract

    var path: String {
        switch self {
        case .health: return "/health"
        }
    }

    var method: String {
        switch self {
        case .health: return "GET"
        }
    }
}
```

**Core/Network/APIClient.swift:**
```swift
import Foundation

actor APIClient {
    static let shared = APIClient()

    private let baseURL: URL
    private let session: URLSession

    private init() {
        guard let url = URL(string: Bundle.main.infoDictionary?["API_BASE_URL"] as? String ?? "http://localhost:3000") else {
            fatalError("Invalid API_BASE_URL in Info.plist")
        }
        self.baseURL = url
        self.session = URLSession.shared
    }

    func request<T: Decodable>(_ endpoint: Endpoint) async throws -> T {
        var request = URLRequest(url: baseURL.appendingPathComponent(endpoint.path))
        request.httpMethod = endpoint.method

        // Auth token injection
        if let token = await TokenStorage.shared.getToken() {
            request.setValue("Bearer \(token)", forHTTPHeaderField: "Authorization")
        }

        // Correlation ID
        request.setValue(UUID().uuidString, forHTTPHeaderField: "X-Correlation-ID")

        let (data, response) = try await session.data(for: request)

        guard let httpResponse = response as? HTTPURLResponse,
              (200..<300).contains(httpResponse.statusCode) else {
            throw APIError.invalidResponse
        }

        return try JSONDecoder().decode(T.self, from: data)
    }
}

enum APIError: Error {
    case invalidResponse
    case unauthorized
}
```

---

## Auth — Keychain Token Storage

**Core/Auth/TokenStorage.swift:**
```swift
import Foundation
import Security

actor TokenStorage {
    static let shared = TokenStorage()

    private let tokenKey = "{{component-name}}.auth_token"

    func getToken() -> String? {
        let query: [CFString: Any] = [
            kSecClass: kSecClassGenericPassword,
            kSecAttrAccount: tokenKey,
            kSecReturnData: true,
            kSecMatchLimit: kSecMatchLimitOne
        ]
        var result: AnyObject?
        let status = SecItemCopyMatching(query as CFDictionary, &result)
        guard status == errSecSuccess, let data = result as? Data else { return nil }
        return String(data: data, encoding: .utf8)
    }

    func setToken(_ token: String) {
        guard let data = token.data(using: .utf8) else { return }
        let query: [CFString: Any] = [
            kSecClass: kSecClassGenericPassword,
            kSecAttrAccount: tokenKey,
            kSecValueData: data,
            kSecAttrAccessible: kSecAttrAccessibleWhenUnlocked
        ]
        SecItemDelete(query as CFDictionary)
        SecItemAdd(query as CFDictionary, nil)
    }

    func clearToken() {
        let query: [CFString: Any] = [
            kSecClass: kSecClassGenericPassword,
            kSecAttrAccount: tokenKey
        ]
        SecItemDelete(query as CFDictionary)
    }
}
```

**Core/Auth/AuthManager.swift:**
```swift
import Foundation
import Combine

@MainActor
final class AuthManager: ObservableObject {
    @Published var isAuthenticated: Bool = false

    init() {
        isAuthenticated = TokenStorage.shared.getToken() != nil
    }

    func login(token: String) {
        TokenStorage.shared.setToken(token)
        isAuthenticated = true
    }

    func logout() {
        TokenStorage.shared.clearToken()
        isAuthenticated = false
    }
}
```

---

## Push Notifications

**Core/PushNotifications/PushNotificationManager.swift:**
```swift
import UserNotifications
import UIKit

final class PushNotificationManager: NSObject {
    static let shared = PushNotificationManager()

    func requestPermission() {
        UNUserNotificationCenter.current().requestAuthorization(
            options: [.alert, .badge, .sound]
        ) { granted, error in
            guard granted else { return }
            DispatchQueue.main.async {
                UIApplication.shared.registerForRemoteNotifications()
            }
        }
    }

    // Called from AppDelegate.application(_:didRegisterForRemoteNotificationsWithDeviceToken:)
    func didRegisterWithToken(_ token: Data) {
        let tokenString = token.map { String(format: "%02.2hhx", $0) }.joined()
        // TODO: Send tokenString to backend for storage
        print("[PushNotifications] Device token: \(tokenString)")
    }
}
```

---

## Deep Linking

**Core/DeepLinking/DeepLinkHandler.swift:**
```swift
import Foundation

final class DeepLinkHandler {
    static let shared = DeepLinkHandler()

    func handle(url: URL) {
        // URL scheme: {{deep-linking-scheme}}://
        // Universal links: associated domain from manifest
        guard let components = URLComponents(url: url, resolvingAgainstBaseURL: true) else { return }

        // TODO: Add route matching per SDL deep_linking config
        switch components.path {
        default:
            print("[DeepLink] Unhandled path: \(components.path)")
        }
    }
}
```

---

## Info.plist keys (add to Info.plist)

```xml
<!-- API base URL — set per build configuration -->
<key>API_BASE_URL</key>
<string>$(API_BASE_URL)</string>

<!-- Deep linking URL scheme -->
<key>CFBundleURLTypes</key>
<array>
  <dict>
    <key>CFBundleURLSchemes</key>
    <array>
      <string>{{deep-linking-scheme}}</string>
    </array>
  </dict>
</array>

<!-- Associated domains (universal links) -->
<!-- Add in Signing & Capabilities → Associated Domains: applinks:{{domain}} -->

<!-- Permissions — add only what the app uses -->
<!-- <key>NSCameraUsageDescription</key>
<string>{{camera-usage-description}}</string>
<key>NSMicrophoneUsageDescription</key>
<string>{{microphone-usage-description}}</string> -->
```

---

## .gitignore

```
# Xcode
build/
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
xcuserdata/
*.xccheckout
*.moved-to-trash
*.xcuserstate
*.xcscmblueprint
.DS_Store

# SPM
.build/
.swiftpm/

# CocoaPods (if used)
Pods/

# Fastlane
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/**/*.png
fastlane/test_output
```

---

## CI Workflow

**.github/workflows/ci.yml:**
```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode_16.app

      - name: Build
        run: |
          xcodebuild clean build \
            -project {{ComponentName}}.xcodeproj \
            -scheme {{ComponentName}} \
            -destination "generic/platform=iOS Simulator" \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO | xcpretty

      - name: Test
        run: |
          xcodebuild test \
            -project {{ComponentName}}.xcodeproj \
            -scheme {{ComponentName}} \
            -destination "platform=iOS Simulator,name=iPhone 16" \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO | xcpretty
```
